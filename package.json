{
  "name": "cypress-todo",
  "version": "1.0.0",
  "description": "Cypress testing a TodoMVC",
  "main": "index.html",
  "scripts": {
    "start": "npm run serve",
    "serve": "http-server -c-1 -p 8080",
    "cypress": "cypress open",
    "test": "start-server-and-test start http://localhost:8080 cypress run"
  },
  "dependencies": {
    "cypress": "^10.3.0",
    "cypress-axe": "^1.0.0",
    "http-server": "^14.1.1",
    "start-server-and-test": "^1.14.0"
  }
}
